name: Deploy to file share

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  DeployFiles:
    runs-on: [self-hosted, windows]
    
    steps:
      - name: Pull Repo
        run: cd /D U:\actions_test\git_actions
        shell: cmd